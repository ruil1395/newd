# Qwen Code Voice Bot 🎤

Telegram-бот для голосового управления **Qwen Code** с системой обратной связи.

## 🎯 Возможности

1. **🎤 Голосовые сообщения** — отправляйте голосовые сообщения в Telegram
2. **🔄 Speech-to-Text** — конвертация голоса в текст через OpenAI Whisper API
3. **🤖 Qwen Code интеграция** — отправка промтов в Qwen Code (запущен в GitHub Codespaces)
4. **📬 Ответы** — получение ответов от Qwen Code в Telegram
5. **⭐ Обратная связь** — система оценок, комментариев и уточнений

## 📁 Структура проекта

```
/workspaces/newd/
├── bot.py              # Основной код бота
├── requirements.txt    # Python зависимости
├── .env.example        # Шаблон переменных окружения
├── .env                # Ваши переменные окружения (не коммитить!)
└── README.md           # Документация
```

## 🚀 Установка и запуск

### 1. Клонирование и установка зависимостей

```bash
cd /workspaces/newd

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте ваши ключи:

```env
# Telegram Bot Token (получить у @BotFather)
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# OpenAI API Key для Whisper STT
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxx

# Qwen Code API URL
# Для GitHub Codespaces: https://<your-codespace>-8080.app.github.dev/api
# Для локального запуска: http://localhost:8080/api
QWEN_CODE_API_URL=http://localhost:8080/api
```

### 3. Получение Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env`

### 4. Получение OpenAI API Key

1. Перейдите на [platform.openai.com](https://platform.openai.com/api-keys)
2. Создайте новый API ключ
3. Скопируйте ключ в `.env`

### 5. Запуск бота

```bash
python bot.py
```

## 📱 Использование

### Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Запустить бота |
| `/help` | Справка по использованию |
| `/history` | Показать историю диалогов |
| `/clear` | Очистить историю |
| `/settings` | Настройки |

### Голосовые сообщения

1. Нажмите и удерживайте кнопку микрофона в Telegram
2. Скажите ваш запрос для Qwen Code
3. Отправьте сообщение
4. Бот распознает голос и отправит в Qwen Code
5. Получите ответ и оцените его

### Система обратной связи

После получения ответа от Qwen Code вы можете:

- **⭐ Оценить ответ** — от 1 до 5 звёзд
- **💬 Оставить комментарий** — написать текстовый отзыв
- **❓ Уточнить ответ** — задать уточняющий вопрос

## 🔧 Настройка Qwen Code API

### Для GitHub Codespaces

Если Qwen Code запущен в GitHub Codespaces на порту 8080:

```env
QWEN_CODE_API_URL=https://<your-codespace-id>-8080.app.github.dev/api
```

### Локальный запуск

```env
QWEN_CODE_API_URL=http://localhost:8080/api
```

### Ожидаемый формат API

Бот отправляет POST-запрос с JSON:

```json
{
    "prompt": "текст запроса",
    "history": [...],
    "timestamp": "2024-01-01T00:00:00"
}
```

Ожидает ответ:

```json
{
    "response": "ответ от Qwen Code"
}
```

## 📊 Логирование

Бот использует стандартное логирование Python. Логи выводятся в консоль в формате:

```
2024-01-01 12:00:00 - bot - INFO - Received voice message from user 123456
```

## 🔒 Безопасность

- **Никогда не коммитьте файл `.env`** в репозиторий
- Добавьте `.env` в `.gitignore`
- Храните API ключи в секрете

## 🛠 Troubleshooting

### Бот не запускается

Проверьте наличие всех переменных окружения:

```bash
python -c "from dotenv import load_dotenv; load_dotenv(); import os; print(os.getenv('TELEGRAM_BOT_TOKEN'))"
```

### Ошибка распознавания голоса

- Проверьте наличие `OPENAI_API_KEY` в `.env`
- Убедитесь, что на счету OpenAI есть средства
- Проверьте формат голосового сообщения (должно быть OGG/OPUS)

### Ошибка подключения к Qwen Code

- Проверьте доступность API URL
- Убедитесь, что Qwen Code запущен на указанном порту
- Для GitHub Codespaces проверьте, что порт 8080 открыт

## 📄 Лицензия

MIT License

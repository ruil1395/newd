# Qwen Code Voice Bot 🎤

Telegram-бот для голосового управления **Qwen Code** с системой обратной связи.

## 🆓 Полностью бесплатное решение

Все компоненты работают **локально и бесплатно**:

| Компонент | Технология | Стоимость |
|-----------|------------|-----------|
| Speech-to-Text | **Vosk** | ✅ Бесплатно, оффлайн |
| Связь с Qwen Code | **Файл-очередь** | ✅ Бесплатно |
| Telegram Bot | **aiogram** | ✅ Бесплатно |

❌ **Не использует:** OpenAI API, Whisper, платные сервисы

## 🎯 Возможности

1. **🎤 Голосовые сообщения** — отправляйте голосовые сообщения в Telegram
2. **🔄 Speech-to-Text** — конвертация голоса в текст через Vosk (оффлайн)
3. **🤖 Qwen Code интеграция** — отправка промтов через файл-очередь
4. **📬 Ответы на русском** — получение ответов от Qwen Code в Telegram
5. **📊 История диалогов** — просмотр последних запросов и ответов

## 📁 Структура проекта

```
/workspaces/newd/
├── bot.py                    # Telegram бот
├── queue_processor.py        # Обработчик очереди для Qwen Code
├── requirements.txt          # Python зависимости
├── .env.example              # Шаблон переменных окружения
├── download_vosk_model.sh    # Скрипт загрузки модели Vosk
├── vosk-model-ru/            # Модель Vosk (скачивается отдельно)
├── /tmp/qwen_queue/          # Очередь входящих запросов
└── /tmp/qwen_responses/      # Очередь исходящих ответов
```

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
cd /workspaces/newd

# Установите Python зависимости
pip install -r requirements.txt

# Установите ffmpeg для обработки аудио
sudo apt-get update && sudo apt-get install -y ffmpeg
```

### 2. Скачивание модели Vosk

```bash
# Сделайте скрипт исполняемым
chmod +x download_vosk_model.sh

# Запустите загрузку (~40 МБ)
./download_vosk_model.sh
```

Или вручную:

```bash
wget https://alphacephei.com/vosk/models/vosk-model-small-ru-0.22.zip
unzip vosk-model-small-ru-0.22.zip
mv vosk-model-small-ru-0.22 vosk-model-ru
```

### 3. Настройка переменных окружения

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Telegram Bot Token (получить у @BotFather)
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Директории для файл-очереди
QUEUE_DIR=/tmp/qwen_queue
RESPONSES_DIR=/tmp/qwen_responses

# Путь к модели Vosk
VOSK_MODEL_PATH=./vosk-model-ru
```

### 4. Получение Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env`

### 5. Запуск бота

Вам нужно запустить **два процесса**:

**Терминал 1 — Telegram бот:**
```bash
python bot.py
```

**Терминал 2 — Обработчик очереди:**
```bash
python queue_processor.py
```

## 📱 Использование

### Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Запустить бота |
| `/help` | Справка по использованию |
| `/history` | Показать историю диалогов |
| `/clear` | Очистить историю |
| `/settings` | Настройки |
| `/status` | Статус системы |

### Голосовые сообщения

1. Нажмите и удерживайте кнопку микрофона в Telegram
2. Скажите ваш запрос для Qwen Code
3. Отправьте сообщение
4. Бот распознает голос (Vosk) и создаст файл в очереди
5. Обработчик прочитает файл и отправит в Qwen Code
6. Получите ответ на русском языке

## 🔧 Интеграция с Qwen Code

### Вариант A: Direct API (рекомендуется)

Если Qwen Code запущен как HTTP-сервис:

```env
QWEN_CODE_API_URL=http://localhost:8080/api
```

### Вариант B: CLI

Если установлен `qwen-code` CLI:

```bash
npm install -g @qwen-code/qwen-code
```

Обработчик очереди автоматически использует CLI.

### Вариант C: Custom Processor

Отредактируйте `queue_processor.py` и добавьте свою логику в функцию `process_with_subprocess()`:

```python
async def process_with_subprocess(request_data: Dict[str, Any]) -> Optional[str]:
    prompt = request_data.get("prompt", "")
    
    # Ваша логика обработки
    # Например, вызов скрипта или обращение к локальной модели
    
    return response_text
```

## 📊 Формат файл-очереди

### Входящий запрос (`/tmp/qwen_queue/{id}.json`)

```json
{
  "id": "uuid-1234-5678",
  "user_id": 123456789,
  "prompt": "текст запроса",
  "history": [...],
  "timestamp": "2024-01-01T00:00:00"
}
```

### Исходящий ответ (`/tmp/qwen_responses/{id}.json`)

```json
{
  "id": "uuid-1234-5678",
  "user_id": 123456789,
  "response": "ответ от Qwen Code",
  "timestamp": "2024-01-01T00:00:01",
  "status": "completed"
}
```

## 🛠 Troubleshooting

### Бот не распознаёт голос

1. Проверьте наличие модели Vosk:
   ```bash
   ls -la vosk-model-ru/
   ```

2. Проверьте статус системы:
   ```
   Отправьте боту: /status
   ```

3. Переустановите модель:
   ```bash
   rm -rf vosk-model-ru
   ./download_vosk_model.sh
   ```

### Ошибка очереди

1. Проверьте директории:
   ```bash
   ls -la /tmp/qwen_queue/
   ls -la /tmp/qwen_responses/
   ```

2. Очистите зависшие файлы:
   ```bash
   rm -f /tmp/qwen_queue/*.json
   rm -f /tmp/qwen_responses/*.json
   ```

3. Перезапустите обработчик очереди

### Проблемы с аудио

Установите ffmpeg:

```bash
# Ubuntu/Debian
sudo apt-get install -y ffmpeg

# macOS
brew install ffmpeg
```

## 📄 Лицензия

MIT License

## 🔗 Полезные ссылки

- [Vosk STT](https://alphacephei.com/vosk/)
- [aiogram документация](https://docs.aiogram.dev/)
- [Qwen Code](https://github.com/QwenLM/qwen-code)

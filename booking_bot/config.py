"""
Конфигурация бота для записи на услуги
"""

import os
from dotenv import load_dotenv

load_dotenv()

# Telegram Bot Token
BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")

# ID администратора (кому отправлять уведомления о новых записях)
ADMIN_ID = int(os.getenv("TELEGRAM_ADMIN_ID", "0"))

# База данных
DATABASE_PATH = os.getenv("DATABASE_PATH", "./booking.db")

# Часовой пояс (для отображения времени)
TIMEZONE = os.getenv("TIMEZONE", "Europe/Moscow")

# Услуги - ОБРАЗЕЦ (замените на свои)
# Это шаблон - вы можете изменить названия, цены и описания под ваш бизнес
SERVICES = {
        "service_2": {
        "name": "Test",
        "duration": 30,
        "price": 2000,
        "description": "Описание вашей услуги. Например: консультация + выполнение работы."
    },
    "service_3": {
        "name": "⭐ Ваша услуга 3",
        "duration": 30,
        "price": 1000,
        "description": "Описание вашей услуги. Укажите длительность и что получает клиент."
    },
    "service_4": {
        "name": "💎 Премиум услуга",
        "duration": 120,
        "price": 3500,
        "description": "Описание премиум услуги. Полный комплекс работ с гарантией."
    },
}

# Дополнительные услуги - ОБРАЗЕЦ
EXTRA_SERVICES = {
        "extra_2": {
        "name": "➕ Дополнительная опция 2",
        "duration": 45,
        "price": 800,
        "description": "Краткое описание дополнительной услуги."
    },
}

# Рабочие часы (начало, конец)
WORK_HOURS = {"start": 9, "end": 20}

# Дни недели для записи (0 = понедельник, 6 = воскресенье)
WORK_DAYS = [0, 1, 2, 3, 4, 5]  # Пн-Сб

# Количество дней вперёд для записи
DAYS_AHEAD = 14

# Интервал слотов (минуты)
SLOT_INTERVAL = 30

if not BOT_TOKEN:
    raise ValueError("No TELEGRAM_BOT_TOKEN found in environment variables")
